# Always on. Your course website -- the reference students are allowed in the
# room. Replace these with yours.
#
# Each entry covers subdomains too, so example.com admits www.example.com.
# If the site is on GitHub Pages, also put its edge IPs in static4.list so it
# stays reachable for a browser using encrypted DNS the router cannot see.
usfca-cs-tools.github.io
cs326-f26.cs.usfca.edu
